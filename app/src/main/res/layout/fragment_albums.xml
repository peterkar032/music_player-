<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Albums">

    <!-- Τίτλος του άλμπουμ "Top 100" -->
    <TextView
        android:id="@+id/albumTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="Top 100"
        android:textSize="24sp"
        android:textColor="@android:color/black"
        android:paddingTop="16dp"
        android:gravity="center" />

    <!-- Κουμπί εικόνας για το "Top 100" Άλμπουμ -->

    <!-- Κεντρικό FrameLayout που περιέχει τα τραγούδια -->

    <ImageButton
        android:id="@+id/top100AlbumImageButton"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_gravity="center"
        android:layout_marginTop="40dp"
        android:background="@null"
        android:contentDescription="Top 100 Album Button"
        android:src="@drawable/top100" />

    <FrameLayout
        android:id="@+id/tracksContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/white"
        android:visibility="gone">

        <!-- ProgressBar που δείχνει την φόρτωση -->
        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center" />

        <!-- RecyclerView για την εμφάνιση των τραγουδιών -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/tracksRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="16dp"
            android:visibility="gone" />
    </FrameLayout>

</FrameLayout>
